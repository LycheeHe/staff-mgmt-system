import { AdminType, StaffType } from '@/type/param.type'
import LoggerService from './logger.service'

export default class LoginService {
  public logger = new LoggerService(LoggerService.name)
  constructor() { }

  public async login() {

  }
}